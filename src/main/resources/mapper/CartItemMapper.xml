<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.eshop.dao.CartItemDao">

    <resultMap id="CartItemResultMap" type="com.example.eshop.entity.CartItem">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="productId" column="product_id"/>
        <result property="quantity" column="quantity"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
        <association property="product" javaType="com.example.eshop.entity.Product">
            <id property="id" column="product_id"/>
            <result property="name" column="name"/>
            <result property="price" column="price"/>
            <result property="imageUrl" column="image_url"/>
            <result property="stock" column="stock"/>
        </association>
    </resultMap>

    <select id="findByUserId" resultMap="CartItemResultMap">
        SELECT ci.*, p.id as product_id, p.name, p.price, p.image_url, p.stock 
        FROM cart_items ci 
        JOIN products p ON ci.product_id = p.id 
        WHERE ci.user_id = #{userId}
        ORDER BY ci.created_at ASC
    </select>

    <select id="findByUserIdAndProductId" resultType="com.example.eshop.entity.CartItem">
        SELECT * FROM cart_items 
        WHERE user_id = #{userId} AND product_id = #{productId}
    </select>

    <insert id="insert" parameterType="com.example.eshop.entity.CartItem" 
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO cart_items (user_id, product_id, quantity) 
        VALUES (#{userId}, #{productId}, #{quantity})
    </insert>

    <update id="updateQuantity" parameterType="com.example.eshop.entity.CartItem">
        UPDATE cart_items SET quantity = #{quantity} WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM cart_items WHERE id = #{id}
    </delete>

    <delete id="deleteByUserId">
        DELETE FROM cart_items WHERE user_id = #{userId}
    </delete>

    <select id="countByUserId" resultType="int">
        SELECT COUNT(*) FROM cart_items WHERE user_id = #{userId}
    </select>

</mapper>
